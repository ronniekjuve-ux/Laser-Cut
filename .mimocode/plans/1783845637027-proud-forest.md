# План: Коммит изменений сегодняшней сессии

## Что сделать
1. `git add -A` — добавить все изменённые и новые файлы
2. `git commit -m "..."` — сохранить локально

## Файлы для коммита

### Backend (изменённые):
- `app/api/v1/router_applications.py` — автосписание по раскладкам
- `app/api/v1/router_warehouse.py` — thickness, JSON vertices, deficit-analysis, deficit-export, split с артикулом-ссылкой
- `app/db/models.py` — thickness в WarehouseItem, vertices как JSON
- `app/schemas/warehouse.py` — thickness в Create/Update
- `scripts/apply_sql_migrations.py` — миграции для thickness, layout_sheets_used
- `frontend/public/sw.js` — network-first для JS/CSS

### Backend (новые):
- `alembic/versions/004_add_warehouse_tables_and_layout_column.py`

### Frontend (новые):
- `frontend/src/pages/Warehouse/RemnantEditor.jsx`
- `frontend/src/pages/Warehouse/WarehouseDeductModal.jsx`
- `frontend/src/pages/Warehouse/WarehouseMovementHistory.jsx`
- `frontend/src/pages/Warehouse/WarehouseReturnModal.jsx`

### Frontend (изменённые):
- `frontend/src/pages/Warehouse/Warehouse.jsx` — упрощённая форма, таблицы "В наличии"/"Списано", превью по клику
- `frontend/src/pages/Deficit.jsx` — полная переделка: анализ дефицита, фильтры, экспорт
- `frontend/src/pages/Applications/ApplicationDetail.jsx` — блок привязки к складу

## Команда
```bash
git add -A
git commit -m "Складской учёт: толщина, резка, дефицит, компактный UI"
```
