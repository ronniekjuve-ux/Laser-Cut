# План: WebSocket + PWA

## Текущее состояние

### WebSocket — РАБОТАЕТ (частично)
- `app/main.py:32-87` — `ConnectionManager` с `send_to_user()` и `broadcast()`
- `app/api/v1/router_applications.py:1555,1622` — вызовы при смене статуса заявки и создании дефицита
- `frontend/src/hooks/useWebSocket.js` — хук с реконнектом
- `frontend/src/components/Layout.jsx:47` — используется для уведомлений

**Проблема**: WebSocket используется только в 2 местах. Нет broadcast при:
- Смене статуса feedback
- Изменении склада
- Обновлении аудит-лога

### PWA — ЕСТЬ, но отключен
- `frontend/public/manifest.json` — настроен
- `frontend/src/components/InstallPWA.jsx` — кнопка установки
- `frontend/public/sw.js` — пустой (service worker отключен)

**Проблема**: Нет кэширования, нет push-уведомлений

---

## Задача 1: Расширить WebSocket

### Что сделать
1. Добавить `broadcast()` в `router_feedback.py` при смене статуса feedback
2. Добавить `broadcast()` в `router_warehouse.py` при изменении склада
3. Добавить `broadcast()` в `router_audit.py` при обновлении смен

### Файлы для изменения
- `app/api/v1/router_feedback.py` — импорт manager, вызов broadcast
- `app/api/v1/router_warehouse.py` — импорт manager, вызов broadcast
- `app/api/v1/router_audit.py` — импорт manager, вызов broadcast

---

## Задача 2: Восстановить PWA

### Что сделать
1. Исправить `sw.js` — добавить кэширование статических ресурсов
2. Добавить регистрацию service worker в `index.html` или `main.jsx`
3. Добавить push-уведомления через Firebase Cloud Messaging (опционально)

### Файлы для изменения
- `frontend/public/sw.js` — добавить логику кэширования
- `frontend/index.html` — добавить регистрацию SW
- `frontend/public/manifest.json` — добавить `start_url`, `scope`

---

## Порядок выполнения

1. **WebSocket** (30 мин)
   - Импорт manager в router_feedback.py
   - broadcast при смене статуса feedback
   - Импорт manager в router_warehouse.py
   - broadcast при изменении склада
   - Импорт manager в router_audit.py
   - broadcast при обновлении смен

2. **PWA** (45 мин)
   - Исправить sw.js с кэшированием
   - Зарегистрировать service worker
   - Добавить scope в manifest.json
   - Проверить установку на мобильном

---

## Верификация

### WebSocket
1. Открыть два браузера с разными пользователями
2. В первом: изменить статус заявки
3. Во втором: проверить что уведомление пришло мгновенно

### PWA
1. Открыть приложение на телефоне
2. Нажать "Установить приложение"
3. Проверить что иконка появилась на рабочем столе
4. Отключить интернет — проверить что приложение работает
